{% import 'components/navigation/macros.twig' as nav %}
<div class="nav__fixed">
    <div class="nav" id="js-navigation">
        <div class="nav__container container">
            <div class="nav__row row">
                {{
                    nav.logo({
                        "class": 'col-6 col-md-4 col-lg-6',
                        "logo": "#{path}/#{logo}",
                        "alt": title
                    })
                }}
                {{
                    nav.menu('col-12 col-md-5 col-lg-3', items)
                }}
                {{
                    nav.addition('2', 'col-6 col-md-3')
                }}
            </div>
        </div>
    </div>
</div>